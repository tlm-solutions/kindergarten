import {HeaderHostDirective} from './header-host.directive';

describe('HeaderHostDirective', () => {
  it('should create an instance', () => {
    const directive = new HeaderHostDirective();
    expect(directive).toBeTruthy();
  });
});
